import { ThemeActions } from "../../models/redux"

export const ACTION_whiteTheme = () => ({ type: ThemeActions.THEME_WHITE })
export const ACTION_blackTheme = () => ({ type: ThemeActions.THEME_BLACK })
